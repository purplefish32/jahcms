{% extends 'ProbesysCmsBundle::admin.html.twig' %}

{% block headline %}
Page list <a href="{{ path('admin_page_new') }}" class="btn">New</a>
{% endblock headline %}

{% block content %}
<div class="span10">
    <table class="table">
        <thead>
            <tr>
                <th>{{ 'page.title'|trans }}</th>
                <th>{{ 'page.author'|trans }}</th>
                <th>Catégory</th>
                <th>Key words</th>
                <th>Date</th>
            </tr>
        </thead>
        <tbody>
        {% for page in pages %}
            <tr>
                <td>
                    <strong><a href="{{ path('page_show', { 'id': page.id }) }}">{{ page.posttitle }}</a> ( {{ page.poststatus }} )</strong>
                    <div>
                        <a href="{{ path('admin_page_edit', { 'id': page.id }) }}">edit</a>
                        <a href="{{ path('admin_page_delete', { 'id': page.id }) }}">delete</a>
                    </div>
                </td>
                <td>Donovan</td>
                <td>All</td>
                <td>All</td>
                <td>Last modified : {% if page.postdate %}{{ page.postmodified|date('d/m/Y') }}{% endif %}</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

{% endblock content %}
